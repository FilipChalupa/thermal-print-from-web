{
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend && npm run build --prefix backend && cp -r frontend/dist backend/public",
    "start": "node backend/dist/index.js"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
